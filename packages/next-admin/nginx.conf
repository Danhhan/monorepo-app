server {
  listen 80;

  root /usr/share/nginx/html;

  index /v2/index.html;

  location / {
    try_files $uri $uri/ /v2/index.html;
  }

  error_page   500 502 503 504  /50x.html;

  location = /50x.html {
    root   /usr/share/nginx/html;
  }
}