import React from 'react';
import { Divider } from '../src';

export const Default = () => {
  return <Divider text="Text" margin="xxl" />;
};

const story = {
  title: 'Layout/Divider',
  component: Divider,
};

export default story;
