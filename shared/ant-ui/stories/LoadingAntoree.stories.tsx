import React from 'react';
import { AntoreeCustomizeLoading } from '../src';

export const Default = () => {
  return <AntoreeCustomizeLoading />;
};

const story = {
  title: 'Animation/Loading',
  component: AntoreeCustomizeLoading,
};

export default story;
